VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_BUFFER_API_KEY=          # Optional — Buffer calls now go through Supabase Edge Function. Set BUFFER_API_KEY as a Supabase secret instead.
VITE_HF_ACCESS_TOKEN=         # Optional — HuggingFace token for AI image generation. Falls back to Pollinations.ai if not set.
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
